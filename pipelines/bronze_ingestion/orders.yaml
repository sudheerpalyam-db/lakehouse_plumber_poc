# Orders Data Ingestion - Bronze Layer
# Ingests order data from CSV files using CloudFiles Auto Loader

pipeline: bronze_ingestion
flowgroup: orders_ingestion

# Use the CSV ingestion template
use_template: csv_ingestion_template
template_parameters:
  table_name: orders
  landing_folder: orders
  delimiter: ","
  has_header: true
