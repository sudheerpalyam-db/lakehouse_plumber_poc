# Generated by LakehousePlumber
# Pipeline: raw_ingestions
# FlowGroup: products_ingestion

from pyspark import pipelines as dp
from pyspark.sql import functions as F
from pyspark.sql.functions import hash

# Pipeline Configuration
PIPELINE_ID = "raw_ingestions"
FLOWGROUP_ID = "products_ingestion"


# ============================================================================
# SOURCE VIEWS
# ============================================================================

# Schema hints for products_cloudfiles table
products_cloudfiles_schema_hints = """
    product_id STRING NOT NULL,
    product_name STRING NOT NULL,
    category STRING,
    price DECIMAL(10,2),
    stock_quantity INT
""".strip().replace(
    "\n", " "
)


@dp.temporary_view()
def v_products_cloudfiles():
    """Load products CSV files from landing volume"""
    df = (
        spark.readStream.format("cloudFiles")
        .option("cloudFiles.format", "csv")
        .option("header", True)
        .option("delimiter", ",")
        .option("cloudFiles.maxFilesPerTrigger", 200)
        .option("cloudFiles.inferColumnTypes", False)
        .option("cloudFiles.schemaEvolutionMode", "addNewColumns")
        .option("cloudFiles.rescuedDataColumn", "_rescued_data")
        .option("cloudFiles.useStrictGlobber", "false")
        .option("ignoreCorruptFiles", "true")
        .option("ignoreMissingFiles", "true")
        .option("cloudFiles.schemaHints", products_cloudfiles_schema_hints)
        .load("/Volumes/spalyam_catalog/raw_data/landing/products/")
    )

    # Add operational metadata columns
    df = df.withColumn("_record_hash", F.xxhash64(*[F.col(c) for c in df.columns]))
    df = df.withColumn(
        "_source_file_modification_time", F.col("_metadata.file_modification_time")
    )
    df = df.withColumn("_source_file_path", F.col("_metadata.file_path"))
    df = df.withColumn("_source_file_size", F.col("_metadata.file_size"))

    return df


# ============================================================================
# TARGET TABLES
# ============================================================================

# Create the streaming table
dp.create_streaming_table(
    name="spalyam_catalog.raw_data.products",
    comment="Streaming table: products",
    table_properties={
        "layer": "raw",
        "source": "csv",
        "delta.autoOptimize.optimizeWrite": "true",
        "delta.enableChangeDataFeed": "true",
        "quality": "bronze",
    },
)


# Define append flow(s)
@dp.append_flow(
    target="spalyam_catalog.raw_data.products",
    name="f_products_cloudfiles",
    comment="Append flow to spalyam_catalog.raw_data.products",
)
def f_products_cloudfiles():
    """Append flow to spalyam_catalog.raw_data.products"""
    # Streaming flow
    df = spark.readStream.table("v_products_cloudfiles")

    return df
