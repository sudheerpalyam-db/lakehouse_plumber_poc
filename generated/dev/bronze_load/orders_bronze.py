# Generated by LakehousePlumber
# Pipeline: bronze_load
# FlowGroup: orders_bronze

from pyspark import pipelines as dp
from pyspark.sql import functions as F

# Pipeline Configuration
PIPELINE_ID = "bronze_load"
FLOWGROUP_ID = "orders_bronze"


# ============================================================================
# SOURCE VIEWS
# ============================================================================


@dp.temporary_view()
def v_orders_raw():
    """Load orders table from raw schema"""
    df = spark.readStream.table("spalyam_catalog.raw_data.orders")

    # Add operational metadata columns
    df = df.withColumn("_processing_timestamp", F.current_timestamp())

    return df


# ============================================================================
# TRANSFORMATION VIEWS
# ============================================================================


@dp.temporary_view(comment="SQL transform: orders_bronze_cleanse")
def v_orders_bronze_cleaned():
    """SQL transform: orders_bronze_cleanse"""
    df = spark.sql(
        """SELECT
  order_id,
  customer_id,
  product_id,
  quantity,
  unit_price,
  order_date,
  status,
  _source_file_path,
  _source_file_size,
  _source_file_modification_time,
  _record_hash,
  _processing_timestamp
FROM stream(v_orders_raw)
WHERE order_id IS NOT NULL"""
    )

    return df


# ============================================================================
# TARGET TABLES
# ============================================================================

# Create the streaming table
dp.create_streaming_table(
    name="spalyam_catalog.bronze.orders",
    comment="Streaming table: orders",
    table_properties={
        "layer": "bronze",
        "source": "raw_data.orders",
        "delta.autoOptimize.optimizeWrite": "true",
        "delta.enableChangeDataFeed": "true",
        "quality": "bronze",
    },
)


# Define append flow(s)
@dp.append_flow(
    target="spalyam_catalog.bronze.orders",
    name="f_orders_bronze",
    comment="Append flow to spalyam_catalog.bronze.orders",
)
def f_orders_bronze():
    """Append flow to spalyam_catalog.bronze.orders"""
    # Streaming flow
    df = spark.readStream.table("v_orders_bronze_cleaned")

    return df
