# Generated by LakehousePlumber
# Pipeline: silver_load
# FlowGroup: customer_silver_dim

from pyspark import pipelines as dp
from pyspark.sql import functions as F

# Pipeline Configuration
PIPELINE_ID = "silver_load"
FLOWGROUP_ID = "customer_silver_dim"


# ============================================================================
# SOURCE VIEWS
# ============================================================================


@dp.temporary_view()
def v_customer_bronze():
    """Load customer table from silver schema"""
    df = spark.readStream.table("spalyam_catalog.bronze.customers")

    # Add operational metadata columns
    df = df.withColumn("_processing_timestamp", F.current_timestamp())

    return df


# ============================================================================
# TARGET TABLES
# ============================================================================

# Create the streaming table for CDC
dp.create_streaming_table(
    name="spalyam_catalog.silver.customer_dim", comment="Streaming table: customer_dim"
)

# CDC mode using auto_cdc
dp.create_auto_cdc_flow(
    target="spalyam_catalog.silver.customer_dim",
    source="v_customer_bronze",
    keys=["customer_id"],
    sequence_by="_source_file_modification_time",
    stored_as_scd_type=2,
    track_history_except_column_list=[
        "_source_file_path",
        "_source_file_size",
        "_source_file_modification_time",
        "_record_hash",
        "_processing_timestamp",
    ],
    ignore_null_updates=True,
)
